services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-20}
      DB_PATH: ${DB_PATH:-./data/app.db}
      UPLOAD_DIR: ${UPLOAD_DIR:-./data/uploads}
